<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
    使用mapper接口形式
    namespace：mapper接口路径
 -->
<mapper namespace="com.bbs.user.mapper.UserMapper">
    <!-- 
        resultMap：自定义查询结果集
             type：结果集类型，可使用map、实体类
             id  ：自定义结果集唯一标识
     -->
<!--     <resultMap type="com.bbs.user.model.DO.UserDO" id="user"> -->
<!--         id字段 -->
<!--         <id property="userId" column="USER_ID" javaType="long" jdbcType="BIGINT" /> -->
<!--         一般属性字段 -->
<!--         <result property="loginName" column="LOGIN_NAME" javaType="string" jdbcType="VARCHAR" /> -->
<!--         <result property="password" column="PASSWORD" javaType="string" jdbcType="VARCHAR" /> -->
<!--     </resultMap> -->
    
    <!-- 
        select：查询方法
            id：查询方法名称，如果使用接口映射定义，当前值须与接口中对应方法名称相同
            parameterType：查询方法参数类型，可以是简单数据类型，集合参数，实体类参数等
            resultMap：返回结果集类型，引用当前映射文件内已定义的一个结果集
    -->
    <select id="findUserById" parameterType="long" resultType="com.bbs.user.model.DO.UserDO">
        select * from T_BBS_USER where USER_ID = #{id}
    </select>
    
    <!-- 
        insert：插入方法
            id：插入方法名称，如果使用接口映射定义，当前值须与接口中对应方法名称相同
            parameterType：插入方法参数类型，可以是简单数据类型，集合参数，实体类参数等
     -->
    <insert id="addUser" parameterType="com.bbs.user.model.DO.UserDO" keyProperty="userId" useGeneratedKeys="true" >
        insert into T_BBS_USER 
        values(#{userId}, #{loginName}, #{password}, #{createBy}, #{createDate}, #{updateBy}, #{updateDate}, #{deleteFlag})
    </insert>
    
    <!-- 
        insert：更新方法
            id：更新方法名称，如果使用接口映射定义，当前值须与接口中对应方法名称相同
            parameterType：更新方法参数类型，可以是简单数据类型，集合参数，实体类参数等
     -->
    <update id="updateUser" parameterType="com.bbs.user.model.DO.UserDO">
        update T_BBS_USER user 
           set user.PASSWORD  = #{password},
               user.UPDATE_BY = #{updateBy}, user.UPDATE_DATE = #{updateDate}
         where user.USER_ID = #{userId}
    </update>
    
    <!-- 
        insert：删除方法（此处delete方法仍然使用update语句，因此delete标签主要是用于标记区别）
            id：删除方法名称，如果使用接口映射定义，当前值须与接口中对应方法名称相同
            parameterType：删除方法参数类型，可以是简单数据类型，集合参数，实体类参数等
     -->
    <delete id="deleteUser" parameterType="long">
        update T_BBS_USER user 
           set user.DELETE_FLAG = -1
         where user.USER_ID = #{id}
    </delete>
</mapper>